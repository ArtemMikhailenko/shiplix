# Telegram destination for /contact form submissions.
# 1. Create a bot via @BotFather -> copy the token
# 2. Send any message to the bot (or add it to a group)
# 3. Get the chat id from https://api.telegram.org/bot<TOKEN>/getUpdates
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
